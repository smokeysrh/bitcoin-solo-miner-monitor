We are currently on v0.9.2 and we are currently working on 0.9.3, which will include many bug fixes. We are researching, debugging, and fixing any bugs we find. We are pushing for a clean, lightweight, responsive application. The app works, and we are being very careful not to break any of the functionality, so we are heavily researching each bug before making any changes. 

Always be sure to look for things that already exist so that you dont duplicate things. We want to build, or build upon, reusable components.

We are developing in a Windows environment, use the proper Windows commands to execute tasks:
Add files to git first with git add
Use git update-index --chmod=+x instead of chmod +x
Then set executable permissions through git
This ensures cross-platform compatibility

Prior to starting a task, review the entire tasks list to understand what we've done so far and how the current task fits in to the overall big picture.

Theres an actual miner running on the network that we can use for testing 192.168.1.85. Its already been added to the app so you should see it every time.

Keep a clean and orginized codebase and folder directory. There are folders for guides, summaries, and tests.